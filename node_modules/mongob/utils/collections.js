import mongoose from 'mongoose'

export const userCollection = mongoose.model("myCollection1", new mongoose.Schema({
  title: String, // String is shorthand for {type: String}
  author: String,
  body: String,
}));
